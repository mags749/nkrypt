# nkrypt v2

> Offline-first secure credential vault. AES-256 encrypted at rest. Zero cloud, zero telemetry.

---

## Stack

| Layer          | Tool                             | Reason                                                |
| -------------- | -------------------------------- | ----------------------------------------------------- |
| Framework      | React Native + Expo (SDK 52)     | Cross-platform, Expo Router for file-based navigation |
| Navigation     | Expo Router v4                   | File-based, typed routes, modal support               |
| State          | Zustand v5                       | Selective subscriptions, no provider hell, performant |
| Database       | expo-sqlite + drizzle-orm        | Local-only SQLite with type-safe queries              |
| Encryption     | crypto-js (AES-256-CBC + PBKDF2) | AES-256, key derivation with 100k iterations          |
| Secure Storage | expo-secure-store                | Hardware-backed keychain/keystore for passKey hash    |
| Icons          | @expo/vector-icons (Feather)     | Matches original app icon library                     |

---

## Architecture

```
nkrypt-v2/
├── app/                          # Expo Router screens (file-based routing)
│   ├── _layout.tsx               # Root layout, DB migration, providers
│   ├── index.tsx                 # Splash screen with entrance animation
│   ├── auth/
│   │   ├── index.tsx             # Login screen
│   │   └── setup.tsx             # First-time setup (2-step)
│   ├── folders/
│   │   ├── index.tsx             # Folders list (grouped by category)
│   │   └── [id].tsx              # Folder detail — files list
│   ├── files/
│   │   └── [id].tsx              # File detail — site/user/credentials
│   └── modals/
│       ├── create-folder.tsx     # Create / edit folder
│       ├── create-file.tsx       # Create / edit file entry
│       └── passkey-prompt.tsx    # Reveal / copy credentials prompt
│
└── src/
    ├── app/
    │   └── providers/
    │       └── themeStore.ts     # Zustand theme (light/dark, color tokens)
    │
    ├── features/
    │   ├── auth/
    │   │   └── store/authStore.ts      # Auth state + login/setup/logout
    │   ├── folders/
    │   │   └── store/foldersStore.ts   # Folders CRUD + file counts
    │   └── files/
    │       └── store/filesStore.ts     # Files CRUD + encrypt/decrypt
    │
    ├── infra/
    │   ├── database/
    │   │   ├── schema/index.ts         # Drizzle schema (folders, files, settings)
    │   │   ├── migrations/             # Generated by drizzle-kit
    │   │   └── client.ts              # Singleton DB + migration runner
    │   └── crypto/
    │       └── cryptoService.ts        # AES-256 + PBKDF2 + hash utilities
    │
    └── shared/
        ├── components/
        │   └── ui/
        │       ├── index.tsx           # Button, InputField, Card, SectionHeader
        │       └── NkryptLogo.tsx      # SVG logo + icon components
        ├── constants/
        │   └── design.ts              # Colors, Spacing, Typography, Shadow tokens
        └── types/
            └── index.ts               # All shared TypeScript types
```

---

## Security Model

### Two-Factor Entry

nkrypt requires both **Pass Phrase** and **Pass Key** to log in:

| Credential  | Purpose                       | Storage                                      |
| ----------- | ----------------------------- | -------------------------------------------- |
| Pass Phrase | Login verification only       | PBKDF2 hash in SQLite                        |
| Pass Key    | AES-256 encryption/decryption | PBKDF2 hash in SecureStore (hardware-backed) |

### Encryption Details

- Algorithm: **AES-256-CBC**
- Key derivation: **PBKDF2** with 100,000 iterations, SHA-256, 256-bit key
- IV: **Random 128-bit** per encryption operation
- Salt: **Random 256-bit** per encryption operation
- Output format: `base64(salt[32B] + iv[16B] + ciphertext)`
- **Only the credentials field** is encrypted — site and username are stored plaintext

### What is NEVER stored

- ❌ Pass Phrase plaintext
- ❌ Pass Key plaintext
- ❌ Decrypted credentials at rest
- ❌ Any data in cloud/network

### Memory Safety

- Pass Key is held **in-memory only** during the authenticated session
- Logging out immediately clears the in-memory Pass Key
- On app restart, re-authentication is required

---

## State Management

Follows the **Clear Responsibility Model** from the coding standards:

| State Type            | Tool                                       |
| --------------------- | ------------------------------------------ |
| Server/DB data        | drizzle-orm queries (no cache duplication) |
| Auth session          | `useAuthStore` (Zustand)                   |
| Folders data          | `useFoldersStore` (Zustand)                |
| Files data            | `useFilesStore` (Zustand)                  |
| Theme / UI config     | `useThemeStore` (Zustand)                  |
| Local component state | `useState` / `useReducer`                  |

---

## Database Schema

```sql
-- folders
CREATE TABLE folders (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  category TEXT NOT NULL DEFAULT 'General',
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL
);

-- files (cascade delete with folder)
CREATE TABLE files (
  id TEXT PRIMARY KEY,
  folder_id TEXT NOT NULL REFERENCES folders(id) ON DELETE CASCADE,
  site TEXT NOT NULL,
  username TEXT NOT NULL,
  encrypted_credentials TEXT NOT NULL,  -- AES-256 ciphertext
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL
);

-- settings (key-value: passphrase hash, salt, setup flag)
CREATE TABLE settings (
  key TEXT PRIMARY KEY,
  value TEXT NOT NULL,
  updated_at INTEGER NOT NULL
);
```

---

## Getting Started

```bash
# Install dependencies
npm install

# Generate drizzle migrations
npm run db:generate

# Start Expo
npm start

# iOS
npm run ios

# Android
npm run android
```

### Required additional package for module resolver:

```bash
npm install --save-dev babel-plugin-module-resolver
```

---

## Feature Screens

| Screen          | Route                    | Description                           |
| --------------- | ------------------------ | ------------------------------------- |
| Splash          | `/`                      | Animated logo, detects setup status   |
| Setup           | `/auth/setup`            | 2-step first-time credential creation |
| Login           | `/auth`                  | Pass Phrase + Pass Key login          |
| Folders         | `/folders`               | Categorized folder list               |
| Folder Detail   | `/folders/[id]`          | Files in folder                       |
| File Detail     | `/files/[id]`            | Site, user, encrypted credentials     |
| Create Folder   | `/modals/create-folder`  | Modal — folder name + category        |
| Create File     | `/modals/create-file`    | Modal — site, user, credentials       |
| Pass Key Prompt | `/modals/passkey-prompt` | Modal — enter pass key to reveal/copy |

---

## Design System

- **Theme**: Monochromatic — `#0A0A0B` on `#FAFAFA` (light) / `#FAFAFA` on `#09090B` (dark)
- **Typography**: Weight-based hierarchy, tracked uppercase labels, geometric spacing
- **Icons**: Feather icon set (via `@expo/vector-icons`)
- **Toggle**: Theme toggle on splash screen (matches original app)

---

## Coding Standards Applied

1. ✅ Feature-based folder structure
2. ✅ Hooks own logic — stores are the single source of truth
3. ✅ View components stay presentational
4. ✅ Encryption is isolated in `infra/crypto` — never touched by UI
5. ✅ Zustand owns all UI + data state
6. ✅ No server state duplication
7. ✅ Explicit naming everywhere
8. ✅ TypeScript strict mode
9. ✅ Separation of concerns: UI ≠ business logic ≠ data layer
10. ✅ Component readability: state → effects → helpers → JSX
